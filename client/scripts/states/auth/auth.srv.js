(function() {
    angular.module('simplejobs')
        .factory('Auth', Auth);

    Auth.$inject = ['$window'];

    function Auth($window) {
        var self

        function UserKlass (){

        }
    }
})();